import { JsonApiDeserializePipe } from './json-api-deserialize.pipe';

describe('JsonApiDeserializePipe', () => {
  it('should be defined', () => {
    expect(new JsonApiDeserializePipe()).toBeDefined();
  });
});
