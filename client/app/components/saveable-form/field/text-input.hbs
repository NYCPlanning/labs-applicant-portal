<Input
  @type="text"
  @value={{@value}}
  ...attributes
/>

{{#if @showCounter}}
  <SaveableForm::CharacterCounter
    @string={{this.value}}
    @maxlength={{if @maxlength @maxlength '250'}}
  />
{{/if}}
